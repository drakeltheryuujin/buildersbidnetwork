<% content_for :top_head, "You've been invited!" %>
<% content_for :salutation do %>Hello <%= @resource.email %>!<% end %>

<p><%= (@resource.invited_by.present? ? @resource.invited_by.name : 'Someone') %> has invited you to <%= root_url %>, you can accept it through the link below.</p>

<p><%= link_to 'Accept invitation', accept_invitation_url(@resource, :invitation_token => @resource.invitation_token), :class => 'btn primary' %></p>

<p>If you don't want to accept the invitation, please ignore this email.<br />
Your account won't be created until you access the link above and set your password.</p>
