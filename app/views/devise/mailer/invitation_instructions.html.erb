<% content_for :top_head, "You've been invited!" %>
<% content_for :salutation do %>Hello <%= @resource.email %>!<% end %>

<p>
  <%= (@resource.invited_by.present? ? @resource.invited_by.name : 'Someone') %> has invited you to 
  <% if @resource.invited_project.present? %>
    bid on their <%= @resource.invited_project.name %> project on
  <% end %>
  BuildersBidNetwork (<%= root_url %>). 
  You can accept the invitation through the link below.
</p>
<% if @resource.message_body.present? %>
  <p><%= raw @resource.message_body %></p>
<% end %>

<p class="message_actions">
<%= link_to 'View Project Details', project_url(@resource.invited_project), :class => 'btn primary' if @resource.invited_project.present? %>
<%= link_to 'Accept invitation', accept_invitation_url(@resource, :invitation_token => @resource.invitation_token), :class => 'btn primary' %>
</p>

<p>If you don't want to accept the invitation, please ignore this email.<br />
Your account won't be created until you access the link above and set your password.</p>
